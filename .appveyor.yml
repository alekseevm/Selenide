image: Ubuntu

stack: jdk 11

branches:
  only:
    - main

build: off #123

install:
  - java -jar ./artifacts/app-card-delivery.jar &

build_script:
  - chmod +x gradlew
  - ./gradlew test -Dselenide.headless=true --info